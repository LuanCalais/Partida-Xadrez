namespace tabuleiro
{
    enum Cor
    {
        // quando não indicamos o número de cada enum ele assume o padrão int a partir de 0 
        Branca,
        Preta, 
        Amarela,
        Azul,
        Vermelha,
        Verde,
        Laranja,
    }
}
